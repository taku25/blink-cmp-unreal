
local M = {}

M.config = {
  -- 各マクロの補完を個別に有効/無効にするためのフラグ
  enable_uclass = true,
  enable_uproperty = true,
  enable_ufunction = true,
  enable_ustruct = true,
  enable_uenum = true,
  enable_uinterface = true,
}


return M
